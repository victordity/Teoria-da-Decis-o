function jx = VND(fobj, sol, nsol)
 
    tamSol = size(sol);
    for i=1:tamSol(1)
        newSol = shakeSolution(sol, i);
        newFobj = Fvalues1(newSol);
        if fobj > newFobj(1,nsol)
            fobj = newFobj;
            sol()
        end
    end
end